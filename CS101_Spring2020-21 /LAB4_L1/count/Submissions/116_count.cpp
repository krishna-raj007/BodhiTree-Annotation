#include <simplecpp>

main_program{
  int x; cin >> x;

  bool found = false;
  for(int i=2; i < x; i++){
        switch(x){
        case 1:(x % i == 0);
        found = true;
        break;
  }
  }
  switch(x){
  case 1:(found); cout << "Composite.\n";
  case 2: cout << "Prime.\n";
}
}
